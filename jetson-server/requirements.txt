# Yapflow Jetson server dependencies
#
# Install with: pip install -r requirements.txt --break-system-packages
# (Jetson's system Python needs --break-system-packages for most pip
# installs — see docs/yapflow-master-plan.md Section 5 for Jetson-
# specific environment notes.)

websockets>=12.0
ollama>=0.3.0
moonshine-voice>=0.0.49
numpy>=1.24

# Only needed if you set EXPECT_RAW_PCM = False in server.py to decode Opus
# server-side instead of on the Mac client. Left commented out by default
# since the recommended default keeps Opus decoding on the Mac side (see
# server.py module docstring).
# opuslib>=3.0.1
